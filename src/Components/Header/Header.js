import React from 'react';
import './Header.css';

function Header() {
    return(
        <header>
            <h1>Todo List</h1>
            <p>3 more to do, 0 done</p>
        </header>
    );
}

export default Header;